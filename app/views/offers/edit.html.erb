<div class="container">
  <div class="row">
    <div class="col-6 border-r">
      <%= render "suggests/content", suggest: @suggest %>
    </div>
    <div class="col-6">
      <p class="center pa-30">下記内容にオファーを編集します</p>
      <%= render "offers/form", path: offer_path(@offer) %>
    </div>
  </div>
</div>